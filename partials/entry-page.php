<?php defined( 'ABSPATH' ) or die( header( 'HTTP/1.0 403 Forbidden' ) );

// NOTE: here is usually in theme overrides, otherwise theme uses `partials/page`

get_template_part( 'partials/page', gtheme_template_group() );
